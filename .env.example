# Public Twitch Client ID, inlined at build time (Vite). Set in CI secrets for
# release builds so normal users never see the Settings field.
# Leave empty for local/dev — paste an ID in Settings → Developer instead.
# Client ID is public-ish (not a Client Secret) but still keep it out of git.
VITE_TWITCH_CLIENT_ID=
